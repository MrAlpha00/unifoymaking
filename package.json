{
  "name": "unifoymaking",
  "version": "1.0.0",
  "private": true,
  "description": "UniFoy Making - Service marketplace (Vite + Express + Neon Postgres).",
  "workspaces": [
    "frontend",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,magenta \"npm:dev:web\" \"npm:dev:api\"",
    "dev:web": "npm --workspace frontend run dev",
    "dev:api": "npm --workspace server run dev",
    "build": "npm --workspace frontend run build && npm --workspace server run build",
    "start": "npm --workspace server start",
    "lint": "npm --workspace frontend run lint && npm --workspace server run lint"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
